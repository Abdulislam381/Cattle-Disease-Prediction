<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions for Camera and External Storage -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/appicon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/appicon"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <!-- Other Activities -->

        <activity android:name=".ContactUsActivity" android:exported="false" />
        <activity android:name=".TreatmentActivity" android:exported="false" />
        <activity android:name=".PredictDiseaseActivity" android:exported="false" />
        <activity android:name=".PastPredictionActivity" android:exported="false" />
        <activity android:name=".PredictionResultsActivity" android:exported="false" />

        <!-- SplashActivity as Launcher -->
        <activity
            android:name=".splashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Make HomeActivity and WelcomeActivity accessible -->
        <activity android:name=".HomeActivity" android:exported="true" />
        <activity android:name=".welcomeActivity" android:exported="true" />

        <!-- Updated: Export signinActivity and signupActivity -->
        <activity android:name=".signinActivity" android:exported="true" />
        <activity android:name=".signupActivity" android:exported="true" />

    </application>
</manifest>
